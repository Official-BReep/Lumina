do(
    define(num, 5),
    define(result,  num),
    define(number, 1),
    define(x, 0),
    while(!=(number, num),
        do(
        define(result, *(result, number)),
        define(number, +(number, 1)),
        )
    ),
    out(result)
)
